<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testapp">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Calculator2"></activity>
        <activity android:name=".CalculatorActivity"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Activity_b" />
        <activity android:name=".Activity_a" />
        <activity android:name=".CurrencyConverterActivity" />
        <activity android:name=".SmsActivity" />
        <activity android:name=".DiceActivity" />
        <activity android:name=".LoginFormActivity" />
        <activity
            android:name=".ui.login.LoginFormActivity"
            android:label="@string/title_activity_login_form" />
        <activity android:name=".MyBioLinearLayout" />
        <activity android:name=".MyBioRelativeLayout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>